const rotation = { x: 3, y: 4, z: 2 };

console.log('X rotation is ' + rotation.x + ' cm.');

const { x, y, z } = rotation;

console.log('X rotation is ' + x + ' cm.');

const rotation2 = { a: 3, b: 4, c: 2 };

const { b, a, c } = rotation2;

console.log('The value of a is: ' + a);

const { e, d, f } = rotation2;

console.log('The value of d is: ' + d);